---
name: umpire
description: Start a UMPIRE editor rep
argument-hint: "[topic problem]"
agent: 'Interviewer'
---

<!-- GENERATED by scripts/gen_agent_instructions.py -->

Start a UMPIRE rep. Parse arguments:

- No arguments: run `just practice-start umpire`.
- Exactly two arguments, each matching `[a-z][a-z0-9_]*`: append both to that command.
- Anything else: show `/umpire [topic problem]` and run no command.

Never silently draw after malformed arguments. On success, relay the
editor-open result plus `STATE:`, `SOURCE:`, `TEST:`, and `NEXT:` lines. Then
stop. Only the recipe seeds files. Never edit the candidate workspace.
